# OpenAI API key cho chế độ LIVE.
# Sao chép file này thành .env rồi dán key thật vào.
# Không có key -> agent tự động chạy ở chế độ MOCK (autograder vẫn 8/8 PASS).
OPENAI_API_KEY=sk-your-key-here

# Model dùng cho cả baseline và agent (tuỳ chọn).
OPENAI_MODEL=gpt-4o-mini
